# 踩坑紀錄與疑難排解指南 (Troubleshooting) - html-slide-builder

本文檔記錄了 `html-slide-builder` 技能在實際使用、工程串接與邊界條件測試中發現的高頻地雷、失敗案例與標準避坑解法。

---

## 🧭 常見問題索引與排查手冊

### ⚠️ 陷阱 1：投影幕色差導致淺灰色字看不見

#### 1. 錯誤現象與觸發場景
內文一律採用 `#FFFFFF` 或 `#F3F4F6`，禁止在深底使用中灰色字。

#### 2. 根本原因剖析 (Root Cause)
在 AI 生成或外部 API 呼叫過程中，由於上下文長度、特定參數缺失或邊界條件未被防呆機制捕捉所致。

#### 3. 標準避坑與修復方案 (Solution)
- 嚴格依照 `SKILL.md` 中定義的 SOP 步驟執行。
- 使用前確保前置依賴環境與參數完整。
- 若發生異常，可指定更明確的約束提示詞重新執行。

---
### ⚠️ 陷阱 2：Firebase API Key 暴露在公開前端

#### 1. 錯誤現象與觸發場景
配置 Firebase Security Rules，僅允許匿名寫入投票集合，禁止任意讀寫全庫。

#### 2. 根本原因剖析 (Root Cause)
在 AI 生成或外部 API 呼叫過程中，由於上下文長度、特定參數缺失或邊界條件未被防呆機制捕捉所致。

#### 3. 標準避坑與修復方案 (Solution)
- 嚴格依照 `SKILL.md` 中定義的 SOP 步驟執行。
- 使用前確保前置依賴環境與參數完整。
- 若發生異常，可指定更明確的約束提示詞重新執行。

---
### ⚠️ 陷阱 3：手機當遙控器時連線中斷

#### 1. 錯誤現象與觸發場景
使用 WebSocket / Firebase Realtime Database 實現毫秒級翻頁同步。

#### 2. 根本原因剖析 (Root Cause)
在 AI 生成或外部 API 呼叫過程中，由於上下文長度、特定參數缺失或邊界條件未被防呆機制捕捉所致。

#### 3. 標準避坑與修復方案 (Solution)
- 嚴格依照 `SKILL.md` 中定義的 SOP 步驟執行。
- 使用前確保前置依賴環境與參數完整。
- 若發生異常，可指定更明確的約束提示詞重新執行。

---
## 🛠️ 自檢清單 (Checklist Before Running)

- [ ] 確認觸發關鍵詞正確無拼寫錯誤。
- [ ] 確認輸入的素材完整（無缺漏重要欄位或長度過短）。
- [ ] 若涉及外部服務（Notion, Google Calendar, GitHub, Firebase），確認認證 Token 處於有效狀態。
- [ ] 檢查輸出格式是否符合預期，若有異常可隨時對 AI 提出微調反饋。

---

*最後更新時間：2026-09-17*
